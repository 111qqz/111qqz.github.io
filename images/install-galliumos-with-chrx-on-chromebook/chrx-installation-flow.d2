grid-columns: 2
grid-gap: 25

dev_mode: "1. 开启开发者模式 (Developer Mode)\n按 ESC + F3(Refresh) + Power 触发恢复模式\n关闭系统验证 (OS verification is OFF)" {
  shape: rectangle
  style.stroke: "#2563eb"
  style.bold: true
}

fw_flash: "2. 刷入引导固件 (MrChromebox)\n进入 ChromeOS shell 执行固件脚本\n写入 RW_LEGACY 固件以支持 SeaBIOS 引导" {
  shape: rectangle
  style.stroke: "#0891b2"
  style.bold: true
}

chrx_part: "3. 运行 chrx 初次分区\n执行 curl -Os https://chrx.org/go && sh go\n调整内部磁盘大小，划分 Linux 存储分区" {
  shape: rectangle
  style.stroke: "#f59e0b"
  style.bold: true
}

reboot_inst: "4. 重启后正式安装 GalliumOS\n重启再次进入 shell 运行 chrx 安装器\n拉取并解包 GalliumOS 定制内核与镜像" {
  shape: rectangle
  style.stroke: "#6366f1"
  style.bold: true
}

dual_boot: "5. 双系统引导就绪\n开机白屏提示界面按快捷键选择：\n• CTRL+L: 引导 GalliumOS (SeaBIOS)\n• CTRL+D: 引导原生 ChromeOS" {
  shape: rectangle
  style.stroke: "#16a34a"
  style.bold: true
}

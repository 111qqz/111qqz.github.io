direction: down

title: "局部概率密度 f(x) 的极限定义机制 (Window Shrinking Limit)" {
  style.bold: true
  style.stroke-width: 2
  style.stroke: "#2563eb"
}

limit_process: "" {
  grid-columns: 2
  style.stroke: transparent

  s1: "1. 选定目标对称邻域滑窗\n以目标点 x 为中心取对称微元:\nI_h = [x - h/2, x + h/2]\n区间物理宽度为 h" {
    style.stroke: "#2563eb"
    style.stroke-width: 2
  }

  s2: "2. 测量区间真实累积概率\n计算区间内的无量纲概率质量:\nP(I_h) = P(x - h/2 < X < x + h/2)\n(有限区间具有非零确定概率)" {
    style.stroke: "#f59e0b"
    style.stroke-width: 2
    style.bold: true
  }

  s3: "3. 求解区间宏观平均密度\n平均密度 = P(I_h) / h\n单位量纲: 1 / 物理长度\n反映滑窗内的平均概率稠密程度" {
    style.stroke: "#f59e0b"
    style.stroke-width: 2
  }

  s4: "4. 滑窗宽度逼近极限 (h → 0)\n平均密度逼近局部瞬时密度:\nf(x) = lim_{h → 0} P(I_h) / h\n(瞬时概率浓度，绝非单点概率)" {
    style.stroke: "#16a34a"
    style.stroke-width: 3
    style.bold: true
  }

  s1 -> s2: "宽度 h 赋予空间尺度"
  s2 -> s3: "概率除以宽度"
  s3 -> s4: "极限无限缩窄 (h → 0)"
}

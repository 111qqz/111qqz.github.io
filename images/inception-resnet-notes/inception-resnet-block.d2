direction: down

input: 输入特征图
inception: Inception 块\n传统层顶部接 batch normalization
conv: 1×1 卷积\n无激活函数，调整维度
scale: 残差缩放\n因子 0.1–0.3
add: 与输入相加
relu: ReLU 激活

input -> inception -> conv -> scale -> add
input -> add: 残差连接
add -> relu

conv.style.stroke: "#2563eb"
add.style.stroke: "#16a34a"
add.style.bold: true
add.style.stroke-width: 3

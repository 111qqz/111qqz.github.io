direction: down

Number-Level\n上一篇: {
  style.stroke: blue
  固定 bit budget -> Exponent ↔ Fraction -> Range ↔ Precision
}

Graph-Level\n本篇: {
  style.stroke: green
  固定高精度 budget -> Layer A ↔ Layer B ↔ Layer C -> Accuracy ↔ Efficiency
}

Number-Level\n上一篇 -> Graph-Level\n本篇: 相同的 trade-off 思想 {
  style.stroke-dash: 4
}
